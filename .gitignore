# Local dev artifacts — the add-on's /data volume falls back to chickadee/data
# when run outside HAOS; it holds secrets (bridge secret, JWT, BYO keys).
chickadee/data/
chickadee-dev/data/
node_modules/
__pycache__/
